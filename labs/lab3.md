pwd = print work directory
A directory is basically a file or gorup of files or folders
My directory: /home/users/lhi1001/gen711-811
ls = list
This command will list all the folders inside of that directory
ls ' = This means a quote
Control C cancels the current command in quote that I was running.
Blue hightlights are directories, and Gray highlights are just files.
ls -F = Shows us a slash if the blue highlights are actually directories.
man ls = Gives you the full Operators Manual for that command.


q = Kicks you out of manual, and back to the same Terminal Command Line.
ls -lrth = Printed a Bunch of extra information, like the date it waqs created, its owner, etc.
cd = Change Directory Command
head then tab = Used to open files inside of directories, but jsut the first couple of lines.
cd ../ = One Step back out of the directory.
cd ../../ = Two Steps Back Out of the directory.
conda = environment manager (Not needed for lesson) (Hold on to this)

The home and user directoryies are so far out, that it is what everybody is using to access RON
cd ~ = short hand for home directory, up to MY PERSONAL ACCOUNT!
Absolute Path = used to show the entire path with every directory
Tab Complete = Helps type everything at once for you.
rm = Remove or Delete Command
rm -fR "Repository Name" = Delete the reposotory and everything inside of it.
echo $x = Prints the information up to the directory.
history = The complete history of all commands run from that terminal.
grep = A search command for anythign specific.
'^@' = beginnning of the line.
Wildcard Shortcuts help you be more specific in what you're looking for.
/bin - binarys stored on RON
| = pipeline command
| wc = Word count for everything in the directory.


Mandatory arguments to long options are mandatory for short options too.

       -a, --all
              do not ignore entries starting with .

       -A, --almost-all
              do not list implied . and ..

       --author
              with -l, print the author of each file

       -b, --escape
              print C-style escapes for nongraphic characters

       --block-size=SIZE
              with -l, scale sizes by SIZE when printing them; e.g., '--block-size=M'; see SIZE format below

       -B, --ignore-backups
              do not list implied entries ending with ~



